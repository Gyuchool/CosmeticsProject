server:
  address: localhost
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cosmetics?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: j6k2bn@759153
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      dialect: org.hibernate.dialect.MySQLDialect
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug